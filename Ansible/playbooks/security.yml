---
- hosts: all
  become: true

  vars_files:
    - vars/main.yml

  pre_tasks:
    - name: update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  remote_user: root
  roles:
    - security